import { DragonDirective } from './dragon.directive';

describe('DragonDirective', () => {
  it('should create an instance', () => {
    const directive = new DragonDirective();
    expect(directive).toBeTruthy();
  });
});
